# Server config
server:
  port: ${PORT:8081}
# Eureka config

eureka:
  instance:
    hostname: AUTHORS
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}   

# Ribbon config
#ribbon:
#  eureka:
#    enabled: true